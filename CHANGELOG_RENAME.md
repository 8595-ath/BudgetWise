# BudgetWise Rename Changes

This archive contains the following project-cleanup changes:

- Renamed the outer project to **BudgetWise**.
- Renamed the application source folder from `finpulse` to `budgetwise`.
- Updated visible frontend branding, browser title, web manifest, favicon/PWA icons, email subjects, trip invitations, and API service name.
- Updated package names to `budgetwise-frontend` and `budgetwise-backend`.
- Added working backend `npm start` and `npm run dev` scripts.
- Updated the default local database name to `budgetwise`.
- Changed browser storage keys from `pf-*` to `bw-*`.
- Removed copied dependency directories, Git history/remote configuration, real `.env` files, an unused duplicate backend copy, and an unused root npm package.
- Added Windows setup instructions and transparent source attribution.

The functional feature set was not rewritten. Review and understand the code before presenting it in an interview.
